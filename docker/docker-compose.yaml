version: '3.8'

services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password@123
      MYSQL_DATABASE: myapp
      MYSQL_USER: sbaitman
      MYSQL_PASSWORD: SBaitman@123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
volumes:
  db_data: